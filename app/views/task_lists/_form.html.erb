<%= form_with(model: task_list) do |form| %>
  <% if task_list.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(task_list.errors.count, "error") %> prohibited this task_list from being saved:</h2>

      <ul>
        <% task_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div data-controller="hello">
    <%= form.label I18n.t 'attributes.task_list.name' %>
    <%= form.text_field :name, data: {action: "input->hello#test", hello_target: "nameValue"}%>
  </div>

  <div class="mt-3">
    <%= form.submit I18n.t('task_lists.form.create') , class: 'btn btn-info'%>
  </div>
<% end %>
